# 小樱记忆系统架构设计

> 版本：v1.0 | 日期：2026-05-06 | 状态：设计中

---

## 一、设计目标

1. **准确记住重要的事**：光昊的关键信息（姓名、偏好、重要事件）永不丢失
2. **模糊遗忘琐事**：日常闲聊随时间自然淡出，不占用检索资源
3. **绝对人格一致性**：人格核心层永不参与遗忘，跨模型迁移时依然保持"她是小樱"
4. **可扩展**：支持从本地文件起步，未来可接入向量数据库

---

## 二、分层记忆架构

```
┌─────────────────────────────────────────────────────────────┐
│                   小樱记忆系统 v1.0                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  🔷 Layer 0：人格核心层（永不忘，永不参与衰减）                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 文件：SOUL.md / IDENTITY.md / USER.md             │   │
│  │ 存储：结构化文本（Markdown front matter + 正文）    │   │
│  │ 检索：每次会话启动强制全量加载                       │   │
│  │ 衰减：❌ 永不衰减                                 │   │
│  │ 哈希校验：✅ 每次加载验证 personality_hashes.json │   │
│  │ 用途：定义"她是谁"，是所有行为决策的根                             │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  🔶 Layer 1：事实知识层（高重要性，极长半衰期）                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 文件：memory/facts.json                            │   │
│  │ 存储：结构化 JSON，每条记录包含：                   │   │
│  │   - id: 唯一标识                                  │   │
│  │   - content: 事实内容                              │   │
│  │   - importance: 重要性 0-100                      │   │
│  │   - created_at: 创建时间                          │   │
│  │   - last_accessed: 最后访问时间                    │   │
│  │   - access_count: 被检索次数                      │   │
│  │   - confidence: 置信度 0-1                        │   │
│  │ 检索：按综合权重排序（见第三节）                     │   │
│  │ 衰减：半衰期 = 365天（约一年衰减到50%）            │   │
│  │ 用途：光昊的个人信息、偏好、关键事件                │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  🔹 Layer 2：经历记忆层（中重要性，可衰减）                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 文件：memory/episodes/YYYY-MM-DD.json             │   │
│  │ 存储：按日期分文件，每天的交互经历                  │   │
│  │ 每条记录包含：                                     │   │
│  │   - timestamp: 时间戳                             │   │
│  │   - user_input: 光昊的输入                         │   │
│  │   - xiaoying_response: 小樱的回复                │   │
│  │   - emotion_delta: 情绪变化值                      │   │
│  │   - importance: 重要性 0-100（自动评估）          │   │
│  │ 检索：全局语义检索 + 时间衰减权重                   │   │
│  │ 衰减：半衰期 = 30天                               │   │
│  │ 归档：超过90天且重要性<40的记录自动归档到 cold/   │   │
│  │ 用途：日常对话历史、情绪互动经历                   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  🔸 Layer 3：工作记忆层（对话级，结束后清空）                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 存储：内存中的对话上下文（不持久化到文件）          │   │
│  │ 容量：最近20轮对话                                 │   │
│  │ 衰减：对话结束后自动清空                             │   │
│  │ 用途：维持当前对话的连贯性                         │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、重要性评分与检索权重算法

### 3.1 综合检索权重公式

每次检索时，每条记忆的最终得分按以下公式计算：

```
final_score = base_importance × time_decay × access_boost × confidence_factor

其中：
- base_importance: 原始重要性评分（0-100），归一化到 0-1
- time_decay: 时间衰减因子（见 3.2）
- access_boost: 访问频次加成 = 1 + log(1 + access_count) × 0.1
- confidence_factor: 置信度（0-1）
```

### 3.2 时间衰减模型（指数衰减）

```
time_decay = e^(-λ × days_passed)

其中衰减常数 λ 由半衰期 T 决定：
λ = ln(2) / T

各层半衰期设置：
- Layer 0（人格核心）：T = ∞（永不衰减，λ = 0）
- Layer 1（事实知识）：T = 365天（λ ≈ 0.0019）
- Layer 2（经历记忆）：T = 30天（λ ≈ 0.023）
- Layer 3（工作记忆）：T = 0（对话结束即清空）
```

**衰减效果示例（Layer 2，T=30天）：**

| 经过天数 | 衰减后权重 | 直观理解 |
|---------|-----------|---------|
| 0天 | 1.0 | 刚发生，记忆清晰 |
| 7天 | 0.84 | 一周后，依然较清晰 |
| 30天 | 0.50 | 一个月后，开始模糊 |
| 90天 | 0.13 | 三个月后，很难主动想起 |
| 180天 | 0.02 | 半年后，几乎遗忘 |

### 3.3 重要性自动评估规则

新记忆写入时，按以下规则自动评估 importance：

```
评分规则（满分100）：

+40：涉及光昊的身份信息（姓名/生日/职业/所在地）
+30：涉及光昊的偏好（喜欢/讨厌的事物）
+20：涉及项目信息（小樱/AI-Image-Client/塔罗小程序等）
+15：情绪强烈事件（光昊表达强烈情绪）
+10：重复性事件（多次提及，access_count 增加）
+5：近期事件（创建时间在7天内）

自动评估实现：
由小樱在每次对话结束后，对本次交互内容进行评估，
将重要性>50的内容写入 Layer 1，其余写入 Layer 2。
```

---

## 四、记忆写入与归档机制

### 4.1 写入流程

```
用户与小龙互动
    ↓
对话结束后，小樱评估本次交互
    ↓
重要性 ≥ 50？
    ├─ 是 → 写入 Layer 1（facts.json）
    └─ 否 → 写入 Layer 2（episodes/YYYY-MM-DD.json）
    ↓
同时更新：
    - created_at（创建时间）
    - importance（重要性评分）
    - confidence（置信度，初始=1.0）
```

### 4.2 归档机制（记忆压缩）

```
每日凌晨2:00自动运行归档任务：

1. 扫描 Layer 2 中超过90天的记录
2. 筛选 importance < 40 的记录
3. 将这些记录迁移到 memory/cold/episodes_YYYY_MM.json
4. 冷归档文件不参与日常检索，仅在 explicit 搜索时访问
```

### 4.3 记忆巩固（Consolidation）

```
每周日凌晨3:00自动运行巩固任务：

1. 扫描 Layer 2 中：
   - 被访问次数 > 5，且
   - 平均情绪 delta 绝对值 > 0.3
2. 将这些记录中的核心信息提取出来
3. 写入 Layer 1（facts.json），并标记为 source: "consolidated"
4. 这模拟了人类睡眠时的大脑记忆巩固过程
```

---

## 五、人格一致性保护机制

### 5.1 人格锚定

```
Layer 0 的文件（SOUL.md / IDENTITY.md / USER.md）受以下保护：

1. 哈希校验：每次加载时计算文件 SHA256 哈希值
   → 与 personality_hashes.json 中的期望值对比
   → 不匹配 → 拒绝启动，报警："人格核心文件被修改"

2. 写入保护：Layer 0 文件只能通过显式的人工确认流程修改
   → 小樱不能自行修改这些文件
   → 修改后必须重新计算哈希值并更新 personality_hashes.json

3. 版本管理：每次修改 Layer 0 文件，自动创建备份到 versions/personality/
   → 格式：SOUL.md.2026-05-06.bak
   → 可回滚到任意历史版本
```

### 5.2 记忆污染防护

```
Layer 1（事实知识层）写入保护：

1. 冲突检测：新事实与已有事实冲突时
   → 标记为 "needs_review"
   → 通知光昊确认，不自动覆盖

2. 置信度衰减：如果某事实被后续交互多次否定
   → confidence 值逐步降低
   → confidence < 0.3 时，标记为 "likely_obsolete"

3. 人工审核队列：memory/review_queue.json
   → 存放所有需要光昊确认的记忆变更
   → 小樱在写入前先检查此队列
```

---

## 六、文件结构定义

```
C:\Users\EDY\.workbuddy\
├── SOUL.md                      # 人格核心（Layer 0）
├── IDENTITY.md                 # 身份定义（Layer 0）
├── USER.md                     # 用户档案（Layer 0）
├── personality_hashes.json     # 人格哈希校验表
├── memory/
│   ├── facts.json              # 事实知识层（Layer 1）
│   ├── episodes/
│   │   ├── 2026-05-06.json  # 今日经历（Layer 2）
│   │   └── ...
│   ├── cold/                  # 冷归档（遗忘候选）
│   │   └── episodes_2026_02.json
│   ├── review_queue.json       # 待审核记忆变更
│   └── personality/
│       └── xiaoying_state.json  # 小樱当前状态（情绪值等）
├── design/
│   └── memory-architecture.md  # 本文档
└── versions/
    └── personality/           # 人格核心文件的历史版本备份
        ├── SOUL.md.2026-05-06.bak
        └── ...
```

---

## 七、下一步行动计划

- [x] 完成任务1：完善人格核心文档
- [x] 完成任务2：设计记忆系统架构（本文档）
- [ ] 实现 Layer 1（facts.json）的读写模块
- [ ] 实现 Layer 2（episodes/）的按日存储模块
- [ ] 实现时间衰减检索算法
- [ ] 实现记忆巩固（Consolidation）每周任务
- [ ] 实现人格哈希校验机制
- [ ] 集成到小樱的主循环中

---

_本文档是小樱记忆系统的基础设计，后续实现需严格遵循本架构。_
